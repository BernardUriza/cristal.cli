node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Synced Mixamo binaries (run `npm run sync-assets` to regenerate from ../Mixamo).
# The converted character.glb IS committed so the app works out of the box.
public/models/*
!public/models/character.glb
!public/models/idle.glb
!public/models/walking.glb
!public/models/running.glb
