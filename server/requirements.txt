fastapi>=0.110
uvicorn[standard]>=0.29
# fi-runner is not published to a registry yet — install editable from the
# free-intelligence monorepo checkout (the AIRE backend extra = httpx, the SSE
# HTTP client the door needs; no Anthropic SDK, the turn crosses the network):
#   pip install -e /Users/bernardurizaorozco/Documents/free-intelligence/apps/packages/fi-runner[aire]
# (fi-core is pulled in transitively.)
#
# The AIRE door is configured from the environment (see server/.env.local):
#   AIRE_GATE_URL    https base of the AIRE door
#   AIRE_AUTH_TOKEN  the door's Bearer secret
